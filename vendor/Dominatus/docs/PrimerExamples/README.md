# Primer examples

These files are source artifacts for reusable `PRIMER.context` generation.

- `rust-primer.md`: Rust primer doctrine/rules/pattern restrictions.
- `rust-primer-examples.md`: Good/bad example pairs used to build primer example chunks.

Current dogfood maps representative sections into `artifacts/llm-context-dogfood/primers/rust`.
